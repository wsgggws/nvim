return {
	-- Copy this file to lua/config/local.lua for machine-local settings.
	-- local.lua is ignored by git.
	disable_autoformat_projects = {
		-- legacy_project = true,
	},

	-- Optional advanced hook. Return true to enable autoformat, false to disable it.
	-- autoformat = function(ctx)
	-- 	return ctx.default
	-- end,
}
